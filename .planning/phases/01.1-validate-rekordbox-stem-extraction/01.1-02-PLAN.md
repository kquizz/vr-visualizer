---
phase: 01.1-validate-rekordbox-stem-extraction
plan: 02
type: execute
wave: 2
depends_on:
  - 01.1-01
files_modified:
  - .planning/phases/01.1-validate-rekordbox-stem-extraction/FINDINGS.md
  - audio/rekordbox-stems/drums.wav
  - audio/rekordbox-stems/bass.wav
  - audio/rekordbox-stems/vocals.wav
  - audio/rekordbox-stems/other.wav
autonomous: false
requirements:
  - SPIKE-03
  - SPIKE-04

must_haves:
  truths:
    - "4 stem WAV files captured from Rekordbox via solo-and-capture workflow"
    - "Each captured stem is audibly isolated (drums, bass, vocals, other heard separately)"
    - "Findings document records the complete spike outcome with a go/no-go for Phase 3 approach"
  artifacts:
    - path: "audio/rekordbox-stems/drums.wav"
      provides: "Captured drums stem from Rekordbox"
    - path: "audio/rekordbox-stems/bass.wav"
      provides: "Captured bass stem from Rekordbox"
    - path: "audio/rekordbox-stems/vocals.wav"
      provides: "Captured vocals stem from Rekordbox"
    - path: "audio/rekordbox-stems/other.wav"
      provides: "Captured other/instruments stem from Rekordbox"
    - path: ".planning/phases/01.1-validate-rekordbox-stem-extraction/FINDINGS.md"
      provides: "Complete spike findings with Phase 3 architecture recommendation"
      contains: "## Phase 3 Recommendation"
  key_links:
    - from: "Rekordbox STEM ISO solo mode"
      to: "audio/rekordbox-stems/*.wav"
      via: "BlackHole capture with ffmpeg"
      pattern: "ffmpeg.*avfoundation.*BlackHole"
---

<objective>
Capture all 4 Rekordbox stems via solo-and-capture workflow, assess quality, and document complete findings for Phase 3 architecture decisions.

Purpose: Produce the definitive evidence -- 4 captured stem WAV files -- that proves (or disproves) Rekordbox stem extraction is viable for the VR visualizer. Document findings so Phase 3 planning has a clear go/no-go.
Output: 4 stem WAV files in audio/rekordbox-stems/, completed FINDINGS.md with quality assessment and Phase 3 recommendation.
</objective>

<execution_context>
@/Users/kquillen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kquillen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01.1-validate-rekordbox-stem-extraction/01.1-CONTEXT.md
@.planning/phases/01.1-validate-rekordbox-stem-extraction/01.1-RESEARCH.md
@.planning/phases/01.1-validate-rekordbox-stem-extraction/01.1-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Solo-and-capture all 4 Rekordbox stems</name>
  <files>audio/rekordbox-stems/drums.wav, audio/rekordbox-stems/bass.wav, audio/rekordbox-stems/vocals.wav, audio/rekordbox-stems/other.wav</files>
  <read_first>
    - .planning/phases/01.1-validate-rekordbox-stem-extraction/FINDINGS.md (BlackHole device index from Plan 01)
    - .planning/phases/01.1-validate-rekordbox-stem-extraction/01.1-RESEARCH.md (solo-and-capture workflow, pitfalls to avoid)
  </read_first>
  <action>
Capture each of the 4 Rekordbox stems individually using the solo-and-capture workflow. BlackHole routing must be confirmed working from Plan 01.

Claude automates:
1. Create output directory: `mkdir -p audio/rekordbox-stems`
2. For each stem (drums, bass, vocals, other), run ffmpeg recording command when user signals ready:
   `ffmpeg -f avfoundation -i ":{blackhole_device_index}" -acodec pcm_s16le -ar 44100 -t {duration} audio/rekordbox-stems/{stem}.wav`
   Duration: use track length in seconds (e.g., 240 for a 4-min track) or 60 for a representative section.
3. After each capture, verify non-silent: `sox audio/rekordbox-stems/{stem}.wav -n stats 2>&1 | head -5`
4. After all 4 captured, verify all files exist and are non-silent:
   `for f in audio/rekordbox-stems/*.wav; do echo "=== $f ==="; sox "$f" -n stats 2>&1 | head -8; done`

User must do (Rekordbox GUI required):
1. Pre-flight: Close other audio apps, mute macOS notifications, load test track in Rekordbox Performance mode
2. Wait for Rekordbox stem analysis to complete (CPU indicator)
3. For each stem capture (drums -> bass -> vocals -> other):
   a. Enter STEM ISO mode in Rekordbox, solo the target stem (mute all others)
   b. Cue track to beginning (or desired start point)
   c. Tell Claude "ready for {stem}" -- Claude starts ffmpeg recording
   d. Press play in Rekordbox immediately
   e. Wait for track/section to finish, tell Claude "done" to stop recording
4. After all 4 stems: type "all stems captured"
  </action>
  <verify>
    <automated>ls audio/rekordbox-stems/drums.wav audio/rekordbox-stems/bass.wav audio/rekordbox-stems/vocals.wav audio/rekordbox-stems/other.wav 2>/dev/null && echo "All 4 stem files exist" || echo "Missing stem files"</automated>
  </verify>
  <acceptance_criteria>
    - `ls audio/rekordbox-stems/drums.wav audio/rekordbox-stems/bass.wav audio/rekordbox-stems/vocals.wav audio/rekordbox-stems/other.wav` exits 0
    - Each WAV file is >500KB (non-trivial audio content)
    - sox stats for each file shows RMS level > -60 dB (not silent)
  </acceptance_criteria>
  <done>4 stem WAV files exist in audio/rekordbox-stems/, each non-silent and containing the expected isolated stem audio.</done>
</task>

<task type="auto">
  <name>Task 2: Assess stem quality and write complete findings</name>
  <files>.planning/phases/01.1-validate-rekordbox-stem-extraction/FINDINGS.md</files>
  <read_first>
    - .planning/phases/01.1-validate-rekordbox-stem-extraction/FINDINGS.md (existing cache investigation and BlackHole setup sections)
    - .planning/phases/01.1-validate-rekordbox-stem-extraction/01.1-CONTEXT.md (validation criteria, deferred items)
    - .planning/phases/01.1-validate-rekordbox-stem-extraction/01.1-RESEARCH.md (quality comparison guidance)
  </read_first>
  <action>
Generate audio statistics for each captured stem, then update FINDINGS.md with complete results including Phase 3 recommendation.

Steps:
1. Generate sox stats for each captured Rekordbox stem:
   `for f in audio/rekordbox-stems/*.wav; do echo "=== $(basename $f) ==="; sox "$f" -n stats 2>&1; echo; done`

2. Generate spectrograms for visual comparison (if sox spectrogram available):
   ```
   mkdir -p audio/rekordbox-stems/spectrograms
   for f in audio/rekordbox-stems/*.wav; do
     sox "$f" -n spectrogram -o "audio/rekordbox-stems/spectrograms/$(basename $f .wav)_spectrogram.png" 2>/dev/null
   done
   ```

3. If Demucs test stems exist at audio/stems/test_*.wav, generate comparison stats:
   `for f in audio/stems/test_*.wav; do echo "=== $(basename $f) ==="; sox "$f" -n stats 2>&1; echo; done`

4. Update FINDINGS.md by reading the existing file and appending/replacing these sections:

   `## Solo-and-Capture Results` section containing:
   - Track name used for capture
   - Capture method: "Rekordbox STEM ISO solo -> BlackHole 16ch -> ffmpeg -f avfoundation"
   - Capture settings: "PCM 16-bit, 44100 Hz"
   - Table with columns: Stem, File, Duration, RMS Level, Peak Level, Size (from sox stats)
   - Full sox stats output for each stem

   `## Quality Assessment` section containing:
   - Isolation quality observations derived from sox stats (e.g., energy distribution across stems)
   - Artifact notes from stats (clipping, DC offset, unusual patterns)
   - Comparison to Demucs test stems if available (RMS/peak level comparison)
   - Note: "Subjective listening comparison deferred to Phase 3 per CONTEXT.md"

   `## Phase 3 Recommendation` section containing:
   - Verdict: RECOMMENDED, NOT RECOMMENDED, or CONDITIONAL
   - `### What we learned` with numbered findings: (1) cache investigation result, (2) solo-and-capture result, (3) any additional discoveries
   - `### Recommended Phase 3 architecture` with concrete next steps:
     - If solo-and-capture works: "Use BlackHole solo-and-capture for pre-game stem extraction. Play track 4x, solo each stem, capture to WAV. Feed WAVs to existing Godot stem pipeline via stem_loader.gd."
     - If it fails: "Fall back to Demucs pre-separation (already working in audio/stems/). Rekordbox stems not viable for extraction."
     - For live performance context: "Live real-time stem routing remains blocked -- Rekordbox has single master output. Live visualization requires Demucs real-time processing or mixed-signal FFT."
   - `### Open questions for Phase 3` listing any unresolved questions
  </action>
  <verify>
    <automated>grep -q "## Solo-and-Capture Results" .planning/phases/01.1-validate-rekordbox-stem-extraction/FINDINGS.md && grep -q "## Quality Assessment" .planning/phases/01.1-validate-rekordbox-stem-extraction/FINDINGS.md && grep -q "## Phase 3 Recommendation" .planning/phases/01.1-validate-rekordbox-stem-extraction/FINDINGS.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - FINDINGS.md contains `## Solo-and-Capture Results` with a table showing all 4 stems with Duration, RMS Level, Peak Level, Size
    - FINDINGS.md contains `## Quality Assessment` with observations about isolation quality
    - FINDINGS.md contains `## Phase 3 Recommendation` with explicit RECOMMENDED/NOT RECOMMENDED/CONDITIONAL verdict
    - FINDINGS.md contains `### Recommended Phase 3 architecture` with concrete next steps
    - FINDINGS.md contains sox stats output for each captured stem
  </acceptance_criteria>
  <done>FINDINGS.md is complete with cache investigation, BlackHole setup, capture results, quality assessment, and Phase 3 recommendation. The spike question "Can Rekordbox stems be individually captured?" has a definitive, evidence-backed answer.</done>
</task>

</tasks>

<verification>
- 4 WAV files exist in audio/rekordbox-stems/ (drums, bass, vocals, other)
- Each WAV file is non-silent and contains isolated stem audio
- FINDINGS.md is complete with all sections: cache investigation, BlackHole setup, capture results, quality assessment, Phase 3 recommendation
- Phase 3 recommendation includes a clear go/no-go verdict
</verification>

<success_criteria>
1. 4 stem WAV files captured from Rekordbox exist and are non-silent
2. FINDINGS.md contains definitive Phase 3 architecture recommendation
3. The core spike question is answered: "Can Rekordbox's 4 stem outputs be separately captured as individual audio streams on macOS?" -- YES or NO with evidence
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-validate-rekordbox-stem-extraction/01.1-02-SUMMARY.md`
</output>
